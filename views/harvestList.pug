extends layout

include mixins/_storeCard

block content
  .inner
    if (tab)
      h2 Harvest in #{tab}
    else
      h2 When to Harvest
    ul.tags
      each f in final
        li.tag
          a.tag__link(href=`/harvesting/${f._id}` class=(f._id === tab ? 'tag__link--active' : ''))
            span.tag__text= f._id
            span.tag__count= f.count
    .stores
      each store in stores
        +storeCard(store)